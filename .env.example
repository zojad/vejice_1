# Copy this file to ".env" and set the real API key / flags.
VEJICE_API_KEY=your-api-key-here
# Set to true to bypass the live API and return mock corrections locally.
VEJICE_USE_MOCK=false
# TEMP: set to true to allow API diffs that change more than commas.
VEJICE_ALLOW_NON_COMMA=false
